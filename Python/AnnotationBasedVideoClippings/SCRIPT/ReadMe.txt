So you have to start up jupyter notebook. Open anaconda prompt (AP) and navigate to the current folder (../SCRIPT).
Now enter jupyter notebook, and then a browsers open up and you can select the script. Then you can run the script step for step.

If you dont have jupyter notebook just install it by typing the following in AP:
conda install jupyter notebook